<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.qc.seclass.glm">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.AddProductActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />
        <activity
            android:name=".ui.main.AddClassificationActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />


        <activity
            android:name=".ui.main.ProductActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />
        <activity
            android:name=".ui.main.CartListActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />

        <activity
            android:name=".ui.main.CartListUpdateActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />

    </application>

</manifest>